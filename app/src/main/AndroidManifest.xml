<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KomPC"
        tools:targetApi="31">

        <!-- Actividad principal cambiada a LoginActivity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de MainActivity -->
        <activity android:name=".MainActivity" android:exported="true" />

        <!-- Declaración de otras actividades necesarias -->
        <activity android:name=".SearchProductsActivity" android:exported="true" />
        <activity android:name=".PublishSaleActivity" android:exported="true" />
        <activity android:name=".ViewPurchasesActivity" android:exported="true" />

        <!-- Declaración de la actividad de registro -->
        <activity android:name=".RegisterActivity" android:exported="true" />

    </application>

</manifest>
